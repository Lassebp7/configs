alias ls 'ls -G --color'
alias lsa 'ls -a'

alias h 'history | head -n 20'
alias c 'clear'

alias reload 'source ~/.config/fish/config.fish ; echo Bash config reloaded'

alias now 'date +"%T"'

alias ping 'ping -c 5'

alias .. 'cd ..'
alias cd.. 'cd ..'
alias ... 'cd ../..'
alias .... 'cd ../../..'
